/// <reference types="vite/client" />

/**
 * This project uses only Vite's built-in browser build metadata.
 * EchoRead backend URLs, authentication values, and API keys are intentionally
 * not declared because the standalone extension has no backend configuration.
 */
export {}
